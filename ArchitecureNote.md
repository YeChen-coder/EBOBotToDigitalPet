这里面就是写一些我对于目前看到的东西的理解。

其实不保证正确，也不保证精准，反正就是我个人的一个理解吧。能记住的都是好的。

---

先说一下在大模型里面观察到的问题吧：我发现它不太理解“我”这个概念。

举个例子：有一次使用者问它“你能看到我吗？”，它的回答是看不到。使用者又问“你能看到什么呢？”，AI 这边的回答是“我能看到有一个人的拖鞋、他的腿，还有他一个人在沙发上”。

但它并不知道，这个坐在沙发上的人就是用户口中的“我”

---

其实里面最重要的东西就是 Docker。

先说 Docker：Dockerfile 是用来 build image 的，compose.yaml 是用来 create container 的，这两个还是挺不一样的。

先说 Dockerfile，它里面可以写两种命令：

1. RUN：是在 build image 的时候就自己跑的。
2. CMD：这个命令是被包在 build 出来的 image 里面，只有通过这个 image create container 的时候，container 才会让它自动跑。它们有这么一层递进的关系。

其实就跟所有的 service 差不多，不管你用什么程序写，container 放在那儿，总不可能作为一个死包（只带着文件、进程和网络）待着，得让它动起来。怎么让它动起来呢？它一定得有一条启动命令。对于 Python 来说，不管是 main.py 还是 app.py 都不重要，反正必须得有一个东西让它跑起来，而这个通常都是写在 Dockerfile 里面的

现在的 Docker 一共分三个 container（容器），底下肯定对应三个 image（镜像），这个没什么好说的。

这三个容器是作为一个 compose 部署的。之所以用 compose，是因为它们内部是有连接的，需要一个共同的内部网络来方便进行内部通信。此外，这三个 container 的重启策略（restart policy）都是 unless-stopped。

另外还有一个比较基本的东西：任何一个 container 自己都是一个独立的小包，里面包含了自己的文件、进程和网络。但是一旦 container 被销毁，里面的数据也就没了。

如果有一些内容确实需要持久化保存，就必须在宿主机上找一个位置，然后在 Docker 里面配置挂载，把宿主机上的这个位置映射成 container 内部写入文件的路径。

然后去用来记录日志，记录输入输出的一个日志：输入是 user，输出是 assistant。

这两个日志文件肯定得在我宿主机上待着。因为我这边其实 container 重建挺频繁的，你这一重建，它里边的东西都没了，我就很难再去 debug 了。

还有件事：那个 .env 肯定不在 Docker 里面，必须在 Docker 外面。不然每次改它我还得进到容器里面直接改，那实在太费劲了，所以它一定是在外面。

而且每次对 .env 做了更改之后，就必须重新 recreate 这个 container 了。注意一下，这边虽然有三个 container，但确实只有一个 runtime assistant 的 container 是跟 .env 有关的。

其他两位（Home Assistant 和 ebo-engine）跟 .env 里的 prompt 没关系。我最常出现的情况就是改了 .env 里面的 prompt，也就是里面 role 的 prompt。

所以一般这种情况下，只需要重新生成 runtime assistant 这个 container 就可以了。

这好像是跟文件挂载有关系，不过具体怎么回事我给忘了。

接下来往细了讲：

先说第一个：Home Assistant。这个东西其实真没什么用，正如前面说的，自从不用 LLM Vision 之后，Home Assistant 基本就是开一个面板，里面基本什么都不过。它往外开的端口，是方便坐在宿主机面前的用户可以在浏览器中访问到 Home Assistant 服务，宿主机那边是做了一个端口映射的。

再谈第二个：ebo-engine（这个其实挺有的谈，我觉得顺手一起说了吧）。是这样的，ebo-engine 所做的事情，是去把 EBO 这个厂家他们自己的摄像机跟 EBO Cloud（他们那个云）中间用的私有协议内容进行编解码，然后给包出来。它的音轨仍然是 Opus，视频流变成了 RTSP。这样的话，后续拿到了这么一个东西，就方便作为一个标准流去进行处理了。

好，再谈最后一个，就是 Runtime Assistant 了。

其实它的数据流一定是有两条线的：

1. 一条线是站在外面看的一个来回流向：它要输入机器人那边的音频和视频作为输入，自己在中间进行程序处理，从 OpenAI 那边拿到信息之后，又得把这个信息传回给 ebo-engine。这也是为什么说它们三位，或者至少 ebo-engine 和 Runtime Assistant 这两位，最好在一个内部网络中，所以就把它们放在同一个 Compose 里面。
2. 在 Runtime Assistant 那边它还有一条线：因为毕竟要去调用 API，还要从 API 里面拿到输出，所以它还有一条向外去发请求、调用 API 的线

在之后，以下一切讲到关于 prompt、音频、视频等这些处理，其实全都放在 realtime system 的这个容器代码中。

这边用的是 Python，没有别的原因，就是单纯因为我 Python 更熟一点。用 Python 写，我能看得懂也好修改；如果是用其他语言，我还得再学一门，感觉就有点偏了

---

这个事情讲起来其实有点复杂，因为像这种音视频同步录制、又有持续音频输出的机制，涉及的内容比较多。我先想到什么就说什么吧。

当前这个模型的 prompt 实际上分了四个部分：

1. 任务设定（必须）：定义模型的基础任务。
2. 视角规则：这是 Codex 自己想的，我看 OpenAI 那边其实没有对此做要求。反正我个人对目前的效果不是很满意。
3. 55 分钟轮换交接记忆（可选）：
之所以做这个，是因为 OpenAI 把一个 session 限制在了 60 分钟。60 分钟一过，不管当前 conversation 里有没有说话、有没有内容，它都会强制停掉并自动开一个新 session。
我做这个 55 分钟轮换交接机制，是为了避免在定时关闭并重开 session 时，正好卡在用户说话的间隙，导致体验断裂。它的主要作用就是把这 50 多分钟的内容做个总结，作为 prompt 的输入传给下一个 session 使用。
4. 断网恢复记忆（可选）：
这个主要是针对断网的情况（其实我到现在还没怎么用过，因为我这边网络很好）。程序目前是实时跑在我的 PC 上，家里环境比不上专业服务器，难免遇到宽带断开重连的情况。
我不希望断网导致前面的记录丢失。当然断网期间肯定抓不到输入，这里针对的是断网前、且当前 session 还没到 55 分钟时的那段记录，把它提取出来作为下一个 session 的 prompt 输入。

---

谈到这儿，其实就得谈一下 Realtime 2.1 session 的输入了。因为它是一个多模态模型：

1. Instruction：就是文字和那些 prompt，只要是能看的文字都算。
2. 音频：Realtime session 里的音频是每 100 毫秒（100ms）进行一次 input_audio_buffer_append，把新来的音频作为输入添加到 conversation 里面。（音频这边我最开始看转出来的 transcribe 识别效果不太好，其实额外加了一层 gate，这个待会儿再细说）。
3. 图像：每次要加到 conversation 时，就走 conversation.item.create，把图加到 session 里面。然后这个图，我也给它做了一些 filter 的设定，因为图真的很贵。图的信息量（跟视频、文字和音频都没法比），一张图实在是太贵了

这就很容易涉及到一个问题：context window 挤满了怎么办？它是先进先出（first in, first out），挤满了就会把前面的内容踢出去

---

好的，那谈到这里，我就赶紧把我还能记得的关于音频和图像的一些设定先说了吧。

先说图像，图像比较好办一些。主要考虑到它是放在家里的场景，而在家里它其实是一个稀疏的状态：白天家里没人；晚上哪怕家里有人，由于家人在家中更多是处于静态、stable 的状态，而不是到处动来动去，所以摄像头角度下的场景变换并不频繁，很少有东西在持续变化。

在这种情况下，如果按照固定的每秒抽几帧加到 conversation / context 里面去，就会很亏。所以这边做了一个机制，去对比当前图片跟上一张图片是否有重大区别，设了一个 threshold。这个对比逻辑非常简单粗暴：单纯把图片压缩后转灰度，去跟上一张图做对比，看有多少像素变化。如果跟上一张图一模一样，这张图就直接丢弃；如果不一致，就丢掉上一张，把这张图作为新的标杆继续比。反正怎么快、怎么简单就怎么来，这块不是重点。

当然，传给 OpenAI、加进去的那张图也不可能是原图，还是会做一点压缩。不过压缩出来依然是彩色的，不至于压得太死，否则模型再聪明，看不到细节也没用

---

我们再谈音频部分。

音频非常重要，因为它是真正判定使用者到底想不想让机器人说话的关键重点。视频动就动了，但只有音频里真正有了提问、小机器人听到了合理且真正有效的声音，这时才应该回答。如果因为识别问题随便听到点动静就开始回答，会非常烦人（经过我家人的亲身验证，有点声音就说话确实体验很差，不过后来改了）。

这主要涉及到原本模型自带的 VAD（语音活动检测）。VAD 能够自动检测收到的音频（也就是刚才提到的每 100 毫秒往上发送的 input_audio_buffer_append），它默认静音 650 毫秒就算一句话说完。模型那边原本有设定：检测到声音后是否立即产生 response。

我最开始是开启立即响应的，但后来发现确实不行：transcribe 出来的结果我自己都看不下去，有非常多的空白、完全不是中文的内容，或者只有一两个字。这种内容放在生成场景下根本没法做有效输出。

所以在这个方面做了一套挺折磨的优化，最终采取的办法是：

1. 让那边先做 transcribe。
2. 校验 language 字段，必须是中文。
3. 校验内容，不能为空，且不能只有一两个字。

以这套逻辑作为基准，再去判断要不要让模型产生 response。这样改完之后，不管是在成本还是在使用体验上，都是一个很大的突破。

同样是声音的问题，针对噪音大家注意一下：

OpenAI 服务器那边对噪音的处理，默认的 noise reduction 是不开的（null）。我后来把它改成了 far field。

虽然我家其实不算大，但实际使用状态是：家里人真正要跟它说话时，都会靠近它（近场声音）；相对离它更远的声音，肯定不是跟它说话的场景。我是基于这个来做判断的。

另外 threshold 也放得比较高：原本应该是 0.5，我提到了 0.65。做这个调整也是考虑到家里会开电视、各种东西落地以及人走动的噪音，我是以这些为基准调的。

实测效果非常之好：周末家里全天开着电视，我听着电视声音挺大的，但全程模型一次 response 都没有被误触发，足以证明效果确实很好
